import type { AppSafeAreaEdge } from "./screen.types";

export const DEFAULT_SCREEN_SAFE_AREA_EDGES: readonly AppSafeAreaEdge[] = [
  "top",
  "right",
  "bottom",
  "left",
];

// import type { Edge } from 'react-native-safe-area-context';
